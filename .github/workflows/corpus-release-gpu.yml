name: corpus release (gpu)

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: corpus-release-gpu
  cancel-in-progress: false

jobs:
  build-and-release:
    name: gpu scrape, embed, release
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 720
    env:
      GH_TOKEN: ${{ github.token }}
      LEGAL_MCP_REPO_DIR: ${{ github.workspace }}
      LEGAL_MCP_ATO_PAGES_DIR: ${{ github.workspace }}/../ato_pages
      LEGAL_MCP_FRL_DIR: ${{ github.workspace }}/../frl_pages
      LEGAL_MCP_MODEL_DIR: >-
        ${{ vars.LEGAL_MCP_MODEL_DIR ||
        '/opt/australian-legal-mcp/models/granite-embedding-small-r2' }}
      LEGAL_MCP_GH_REPO: ${{ github.repository }}
      LEGAL_MCP_ATO_MODE: incremental
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30
      - name: Require NVIDIA GPU
        run: |
          command -v nvidia-smi
          nvidia-smi
      - name: Build Rust binary
        run: cargo build --release --locked --features cuda
      - name: Scrape, embed, publish
        run: scripts/maintainer-sync.sh
