---
description: Show ato-mcp corpus version, document count, and chunk count.
---

Run `ato-mcp stats` and show the JSON output to the user. The relevant fields
are `index_version`, `documents`, `chunks`, and `last_update_at`.
